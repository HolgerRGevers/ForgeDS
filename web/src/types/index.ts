// TypeScript interfaces — barrel export
export type {
  BridgeMessage,
  BridgeResponse,
  BridgeStore,
  ConnectionListener,
  ConnectionStatus,
} from "./bridge";

export type {
  RefinedSection,
  RefinedPromptProps,
  PromptInputProps,
} from "./prompt";
