# Phase 7 Implementation Plan — ERM BRD for Zia

**Target output**: `C:\Users\User\OneDrive\Documents\GitHub\Expense_Reimbursement_Manager\docs\brd\zia-brd-2026-04-24.md`

**Sole input contract**: `c:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\docs\superpowers\specs\2026-04-24-erm-brd-design.md` (1,173 lines — the revised spec). The Phase 7 implementer MUST treat the spec as authoritative; all layout, column, cell-encoding, ordering, and assertion rules live there.

## Prerequisites (implementer checks before writing)

- Spec file exists and line count ≥ 1173.
- Research files exist at the paths the spec §2 references.
- Ground-truth .ds exists at the ERM path and is readable.
- Output dir `C:\Users\User\OneDrive\Documents\GitHub\Expense_Reimbursement_Manager\docs\brd\` may not exist; implementer creates it if missing (mkdir, not an Edit).

## Tasks (strictly ordered; single implementer)

1. **Ingest contracts**
   1.1 Read the full spec.
   1.2 Read the reconciled blueprint at `docs/superpowers/research/2026-04-24-erm-brd/4-reconciled-blueprint.md`.
   1.3 Read all 4 research files (3a, 3b, 3c, 3d) plus `0-brief.md`.
   1.4 Keep the .ds file path handy for spot-checks; do NOT re-ingest the full .ds — research 3a is the authoritative extraction (as corrected by the Phase 5 rebuttal).

2. **Create output dir**: `mkdir -p C:\Users\User\OneDrive\Documents\GitHub\Expense_Reimbursement_Manager\docs\brd\` (no-op if exists).

3. **Write BRD §§1–16 in order** per spec §4 section-by-section contract. One `Write` call per section is fine; a single `Write` of the whole file is also fine. Do NOT use Edit — the file is new.

4. **Embedded cross-ref consistency pass**: before emitting final output, implementer verifies inline:
   - Every `WF-NN` reference in §10, §13, §14 exists as an H3 header in §11.
   - Every `@Picklist:<name>` reference in §6 field tables exists as an H3 in §7.
   - Every `BR-NNN` reference in any section exists in §14.
   - Every `ref/pick = lookup→<form>` in §6 has a matching edge in §5.
   - Every footnote superscript `¹²³…` in §9 has a matching explanation beneath the matrix.

5. **Run self-verify pass** against spec §5 completeness assertions A1…A20+:
   - A1: §6 has exactly 6 form H3 subsections.
   - A2: fields across §6 tables sum to 53 (dept 3 + clients 3 + gl_accounts 7 + approval_thresholds 8 + expense_claims 27 + approval_history 5).
   - A3..A20+: enforce remaining assertions from spec §5 verbatim.

6. **Emit summary to orchestrator**:
   - Absolute output path.
   - Output line count + character count.
   - Pass/fail per assertion (Ax: PASS / Ax: FAIL with brief reason).
   - Any assertion that failed: explain + suggest fix (do NOT silently re-attempt; orchestrator decides).

## Assertions that must PASS before task complete

From spec §5 (pasted here for plan-level traceability; spec is source of truth):
- Exactly 6 forms, 53 fields total, 11 reports (incl. kanban+pivot), 5 lookup edges, 9 status values, 8 role rows (7 internal + portal agg), 18 named email templates + 1 inline, 4 Custom APIs, 13 workflows + possible sibling blocks, 5 shared named picklists.
- Every `hod_approval.on_approve` branch split into `WF-NN.A` / `WF-NN.B`.
- `ESG_Category`, `Carbon_Factor`, `GRI_Indicator` DO NOT appear in §6.3 gl_accounts table; their workflow references in WF-07/WF-09 carry `(advisory — not .ds-declared)` suffix.
- §15 lists 5-field schema-workflow divergence + `compliance_config` advisory note.
- §13 email row for inline governance alert has `template_id = INLINE-01`.

## Failure modes & recovery

| Failure | Action |
|---|---|
| Research file missing | Abort. Orchestrator dispatches research backfill. |
| Spec ambiguity discovered mid-write | Flag inline as `<!-- PHASE7-QUESTION: … -->` and continue; orchestrator decides whether to re-spec or accept. |
| Assertion Ax fails on self-verify | Emit failure in summary; orchestrator decides whether to redispatch writer or patch directly. |
| Character/line count blows past 2× spec estimate (>1500 lines, >100k chars) | Halt; orchestrator reviews density-tactic compliance. |

## Post-implementation (orchestrator, not implementer)

- Phase 6 simulation report (see `docs/superpowers/simulations/2026-04-24-erm-brd-sim.md`) must have verdict PASS before running Phase 7.
- Phase 7 summary report posted back to user with: output path, line/char counts, all assertions' pass/fail, and a spot-check suggestion (pick 1 form + 1 workflow from the BRD, compare to .ds ground truth, confirm fidelity).
