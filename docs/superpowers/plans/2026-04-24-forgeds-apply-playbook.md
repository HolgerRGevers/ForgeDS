# forgeds-apply-playbook — Implementation Plan (2026-04-24)

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`. Each task is independently scoped; tasks on the same parallelism tier may be dispatched to separate workers simultaneously. Read the full task before starting — the "Out of scope" section is binding.

**Goal:** Ship `forgeds-apply-playbook`, a zero-dependency CLI that ingests a structured playbook markdown file and a source `.ds` export, validates all form/field/report cross-references, emits syntactically correct Zoho `.ds` workflow-entry and custom-action blocks, and splices them into the `.ds` at the correct insertion points.

**Reference spec:** `docs/superpowers/specs/2026-04-24-forgeds-apply-playbook-design.md`

**Feature branch:** `forgeplan/forgeds-ds-workflow-injector`

**Commit convention:** `feat(apply_playbook): <short description>` — consistent with ForgeDS precedent (`feat(build_ds):`, `feat(widgets):`).

**Prerequisite landing strategy:** Task T0 (DSParser zero-field fix) lands as a direct commit on `main` or as a small fixup PR before the feature branch is merged. Tasks T1–T10 all land on `forgeplan/forgeds-ds-workflow-injector`. The feature branch must not be merged until T0 is confirmed present in the base.

**Test runner:** `pytest tests/test_apply_playbook_*.py -v` — once the first test file exists. Full suite is `pytest tests/ -v`.

**Golden-file discipline:** The `golden_canonical_out.ds` artefact is generated in Task 9 by running the live pipeline against the ERM fixtures, hand-auditing the diff for correctness, and then committing the result. It must never be generated by a test that also asserts against it in the same run.

**Re-import note:** Do NOT attempt to re-import any `.ds` output into live Zoho Creator as part of this plan. Golden-file comparison is the automated acceptance criterion. User re-import is a post-merge manual validation step.

---

## Parallelism map

| Tier | Tasks | Start condition |
|---|---|---|
| 0 | T0-prereq, T1, T2 | Immediately (T0-prereq independent; T1 independent; T2 independent of code but write fixtures) |
| 1 | T3, T4 | After T1 + T2 complete |
| 2 | T5 | After T1 + T2 complete (runs parallel with T3/T4) |
| 3 | T6 | After T1 + T3 + T5 + T0-prereq |
| 4 | T7 | After T3 |
| 5 | T8 | After T5 + T7 |
| 6 | T9 | After all of T0–T8 |
| 7 | T10 | After T9 |

Note on the spec's original build-sequence table (§16): the plan has re-mapped the spec's Task numbering to the brief's 0–10 scheme. Spec Task 0 (fixtures) = plan T2. Spec Task 1 (IR) = plan T3. The prerequisite DSParser fix has no spec task number and is plan T0-prereq.

---

## ### Task T0-prereq — DSParser zero-field FormDef fix

**Depends on:** none

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\parse_ds_export.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_parse_ds_export.py` (existing file — add one test case)

**Implementation notes:** The existing `DSParser._parse_form` method returns `None` when the parsed `fields` list is empty (approximately line 152: `if not fields: return None`). This causes a false DS021 ERROR in the validator when a playbook references a form that has no parseable fields. The fix is a 3-line change: remove or guard the `if not fields: return None` early-return so that a `FormDef` with `fields=[]` is returned instead. Do not change the `FormDef` dataclass signature — `fields` is already typed `list[FieldDef]` and defaults to `[]`. See spec §14 Risk 4 for exact line references.

**Tests to write:** In `tests/test_parse_ds_export.py`, add `test_form_with_no_fields_returns_formdef`: construct a minimal `.ds` snippet containing a form block with no field declarations; assert that `DSParser.parse()` returns a result whose `forms` dict contains the form key (not absent), and that `forms["form_name"].fields == []`. This is the regression guard that must remain green for all subsequent tasks.

**Acceptance criteria:**
- `pytest tests/test_parse_ds_export.py -v` passes with the new test included.
- `DSParser.parse()` on a minimal `.ds` with a zero-field form returns a `FormDef` entry with `fields = []`, not `None`.
- No existing tests broken (the change is narrowly additive — removing a filter, not adding a new code path).
- Commit lands on `main` (or is merge-ready) before Task T6 begins.

**Out of scope for this task:** Any changes outside `parse_ds_export.py` and its test file. No new modules, no CLI changes. Do not build a shadow form-discovery path in `landmarks.py` — fix DSParser directly per spec §14 Risk 4 instruction.

---

## ### Task T1 — Scaffold package + CLI stub

**Depends on:** none

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\__init__.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\cli.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\pyproject.toml`

**Implementation notes:** Create the `apply_playbook` package with an empty `apply_playbook()` stub in `__init__.py` (the real orchestrator is wired in T9). `cli.py` uses `argparse` with three subcommands — `apply`, `validate`, `preview` — each wired to a stub that prints "not yet implemented" and returns exit 0 for now. The entry point in `pyproject.toml` must be `forgeds-apply-playbook = "forgeds.core.apply_playbook.cli:main"` exactly. See spec §4.1 module layout and §11 CLI surface for the full flag set; stub all flags but do not implement any logic. Do not call `load_config()` at module import time per spec §14 Risk 5.

**Tests to write:** No dedicated test file yet — acceptance is verified by the `--help` criterion. Add a smoke test `tests/test_apply_playbook_cli_stub.py` with one test: `test_help_exits_zero` that invokes `forgeds-apply-playbook --help` via `subprocess` and asserts exit code 0 and that the output contains the strings `apply`, `validate`, and `preview`.

**Acceptance criteria:**
- `pip install -e .` completes without errors after `pyproject.toml` edit.
- `forgeds-apply-playbook --help` prints CLI usage and lists all three subcommands.
- `forgeds-apply-playbook apply --help` lists `--ds`, `--playbook`, `--out`, `--force`, `--custom-action-workflow-mode`, `--allow-duplicate-links`, `--allow-reapply`, `--ir-dump`, `--json`, `--dry-run`.
- `forgeds-apply-playbook validate --help` lists `--ds`, `--playbook`, `--json`.
- `forgeds-apply-playbook preview --help` lists `--ds`, `--playbook`, `--custom-action-workflow-mode`, `--json`.
- `pytest tests/test_apply_playbook_cli_stub.py -v` passes.

**Out of scope for this task:** Any real implementation logic. No parser, no validator, no emitter, no mutator. The `apply_playbook()` function in `__init__.py` is a stub only.

---

## ### Task T2 — Fixture generation

**Depends on:** none

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\fixtures\apply_playbook\` (new directory — all files below go here)
- `e2e_basic.ds` — minimal valid `.ds` with two forms, one report, one workflow block
- `e2e_basic_playbook.md` — playbook with one FormAction targeting the above
- `e2e_case_mismatch.ds` — `.ds` where a form field is `merchant_account` (lowercase)
- `e2e_case_mismatch_playbook.md` — playbook referencing `input.Merchant_Account` (wrong case, triggers DS023)
- `e2e_custom_action.ds` — `.ds` with a report backed by a form
- `e2e_custom_action_playbook.md` — playbook with a CustomAction targeting that report
- `minimal_no_workflow.ds` — valid `.ds` with forms + reports but no `workflow { }` block
- `malformed_unclosed_forms.ds` — `.ds` where the `forms { }` block is never closed (triggers DS062)
- `string_escape_displayname.ds` — `.ds` where a form has a display name containing `\"` (empirical escape-convention fixture; see §7.4 study obligation in T5)
- `string_literal_braces.ds` — `.ds` where a Deluge field value or picklist entry contains `{` and `}` inside a double-quoted string
- `orphaned_quote.ds` — `.ds` with an intentionally-orphaned `"` that is never closed (triggers DS063)
- `display_name_unescaped_quote_playbook.md` — playbook where a `name:` bullet contains an unescaped `"` (triggers DS027)
- `unbalanced_parens_playbook.md` — playbook with a Deluge body whose parentheses do not balance (triggers DS028)
- `string_literal_false_positive_playbook.md` — playbook whose Deluge body contains `alert("input.bad_field")` where `bad_field` is not a real field; DS023 must NOT fire
- `erm_snapshot_sha256.ds` — pinned structural copy of the ERM `.ds` (3000+ lines); if the real ERM file exceeds repo size guidance, produce a synthetic equivalent and record its SHA256 in `erm_snapshot_sha256.ds.sha256`

**Implementation notes:** All `.ds` fixtures must be syntactically valid for their intended purpose (except the three intentionally-malformed files). The `erm_snapshot_sha256.ds` fixture is the large-file regression anchor for the landmarks test in T5; it must contain forms with many fields, reports with conditional filter syntax, and at least two string values containing quotes or braces to exercise the escape-convention logic. The `e2e_basic_out.expected.ds` and `e2e_custom_action_out.expected.ds` golden output files are NOT created here — they are generated in T9 after the pipeline works end-to-end. See spec §12 fixture list for the authoritative description of each fixture's structural intent.

**Tests to write:** No test file for this task. Fixtures are consumed by tests in T3–T9. Verify fixtures are syntactically coherent by manual inspection before committing.

**Acceptance criteria:**
- Directory `tests/fixtures/apply_playbook/` exists with all 15 fixture files listed above.
- `e2e_basic.ds` passes a quick manual brace-count check (balanced braces, contains `forms {`, `reports {`).
- `malformed_unclosed_forms.ds` intentionally has unbalanced braces (opening `forms {` with no matching close).
- `orphaned_quote.ds` contains a `"` that is never closed.
- `erm_snapshot_sha256.ds` is >= 500 lines (or a real ERM export); if synthetic, the `.sha256` companion file exists.
- No golden output files (`*_out.expected.ds`) are created yet — those land in T9.

**Out of scope for this task:** Any Python code. No module stubs, no test files. Pure fixture authoring only. Do not attempt to run any ForgeDS tool against these fixtures — they may fail until T5+ lands.

---

## ### Task T3 — IR + diagnostics codes

**Depends on:** T1, T2

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\ir.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\diagnostics_codes.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_ir.py`

**Implementation notes:** `ir.py` contains the five dataclasses from spec §5 verbatim: `DelugeBlock`, `FormAction`, `CustomAction`, `DeferredSection`, `PlaybookIR`. Every field must have a type annotation; `PlaybookIR` must be JSON-serialisable via `dataclasses.asdict()` (no non-serialisable default values). `diagnostics_codes.py` defines module-level string constants for every DS### code in the spec §10 registry (DS001–DS099 range as allocated; reserved codes do not need constants, but all allocated codes do). These constants are imported by parser, validator, emitter, mutator, and landmarks — never hardcoded inline. `ir.py` does NOT import from `diagnostics_codes.py`; the dependency direction is one-way (diagnostics_codes imports nothing from IR).

**Tests to write:** `tests/test_apply_playbook_ir.py` — two test functions: (1) `test_ir_json_roundtrip`: construct a `PlaybookIR` with one `FormAction`, one `CustomAction`, one `DeferredSection`, call `dataclasses.asdict()`, serialize to JSON string, deserialize, and assert all fields round-trip without loss; (2) `test_diagnostics_codes_complete`: import `diagnostics_codes` and assert that constants exist for each allocated DS code from the spec §10 table (DS001, DS002, DS003, DS004, DS005, DS006, DS015, DS021, DS022, DS023, DS025, DS026, DS027, DS028, DS030, DS031, DS032, DS040, DS043, DS050, DS051, DS052, DS053, DS060, DS061, DS062, DS063, DS064, DS065, DS066, DS090, DS091, DS092).

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_ir.py -v` passes both test functions.
- `dataclasses.asdict(PlaybookIR(...))` produces a dict that `json.dumps()` accepts without TypeError.
- `diagnostics_codes.DS001` through `DS092` (allocated range) all exist as importable string constants.
- `ir.py` and `diagnostics_codes.py` have zero external imports (stdlib only: `dataclasses`, `typing`, `__future__`).

**Out of scope for this task:** Parser logic, landmark logic, validation logic. `ir.py` is pure data schema. No `.ds` file is read by this task.

---

## ### Task T4 — Parser

**Depends on:** T1, T2, T3

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\parser.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_parser.py`

**Implementation notes:** `PlaybookParser` accepts raw UTF-8 markdown text and returns a `(PlaybookIR, list[Diagnostic])` tuple. The parser walks lines top-to-bottom using the landmark patterns from spec §6.1: H2 sets form context, H3 begins an action, bullet key-value pairs are extracted with the regex `^[-*]\s+(\w[\w -]+):\s+(.+)$`, fenced ` ```deluge ` blocks are collected, blockquotes immediately after a closing ` ``` ` are attached as `implementation_notes`. Classification logic follows spec §6.2 exactly: `event:` bullet + fenced block → `FormAction`; `report:` bullet + fenced block → `CustomAction`; no fenced block → `DeferredSection` + DS015. The parser tolerates a missing `Success message` bullet in CustomAction sections without emitting an error (spec §4 states this explicitly). All diagnostics use `Severity` and `Diagnostic` from `forgeds._shared.diagnostics`; rule codes come from `diagnostics_codes.py` constants only.

**Tests to write:** `tests/test_apply_playbook_parser.py` covering: (1) valid H2+H3+fenced block parses to `FormAction` with correct `form_name`, `display_name`, `link_name`, `event`, `record_event`; (2) H3 with `report:` bullet parses to `CustomAction`; (3) H3 without fenced block produces `DeferredSection` and DS015 INFO; (4) duplicate `link_name` across two H3 sections produces DS006 ERROR; (5) blockquote line after closing fence appears in `DelugeBlock.implementation_notes`; (6) parse against `tests/fixtures/apply_playbook/e2e_basic_playbook.md` produces exactly one `FormAction` and zero errors; (7) parse against `tests/fixtures/apply_playbook/e2e_custom_action_playbook.md` produces exactly one `CustomAction`.

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_parser.py -v` passes all 7 test functions.
- Parser handles the `e2e_basic_playbook.md` and `e2e_custom_action_playbook.md` fixtures without crashing.
- No import from `forgeds._shared.config` or `load_config()` — parser is config-independent.
- DS015 fires for every H3 section in `e2e_basic_playbook.md` that has no `event:` or `report:` bullet.
- DS006 fires when the same `link_name` appears in two separate H3 sections.

**Out of scope for this task:** Validation against a `.ds` file. The parser operates on markdown only. No `DSLandmarks`, no `DSParser` call, no cross-reference checks.

---

## ### Task T5 — Landmarks

**Depends on:** T1, T2

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\landmarks.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_landmarks.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\docs\superpowers\specs\2026-04-24-forgeds-apply-playbook-design.md` (update §7.4 empirical finding in-place)

**Implementation notes:** Before writing a single line of `landmarks.py`, the implementer MUST open `C:\Users\User\OneDrive\Documents\GitHub\Expense_Reimbursement_Manager\docs\Expense_Reimbursement_Management-development.ds`, locate at least two strings containing `"` or `{`, determine the actual escape convention used (is it `\"` or something else?), and document the finding by editing spec §7.4. Only after completing this empirical study should the state machine be finalised. `DSLandmarks` is a `@dataclass` with the fields from spec §8.1. The state machine is a single pass: `brace_depth` increments on `{` and decrements on `}` except when `in_string` is True; `in_string` toggles on unescaped `"` using the convention confirmed in §7.4. `current_block` tracks which top-level keyword (`forms`, `reports`, `workflow`, `pages`) is open at depth 1. Per-form and per-report body offsets are recorded at the depth transition where the form or report name was last seen. Emits DS060–DS066 diagnostics via `Diagnostic`; error codes come from `diagnostics_codes.py`.

**Tests to write:** `tests/test_apply_playbook_landmarks.py` covering all cases from spec §12: (1) nominal `.ds` (use `e2e_basic.ds`) → all offsets resolve without error; (2) `minimal_no_workflow.ds` → DS064 WARNING emitted, not ERROR, `workflow_open` is None; (3) `malformed_unclosed_forms.ds` → DS062 ERROR; (4) `string_literal_braces.ds` → braces inside the string are not counted; offsets match expected; (5) `orphaned_quote.ds` → DS063 ERROR; (6) `string_escape_displayname.ds` → escaped quote does not toggle `in_string`; offsets remain valid (post-escape-convention empirical study); (7) `test_landmarks_large_file_regression` against `erm_snapshot_sha256.ds` — assert brace balance at EOF, `in_string == False` at EOF, every form from a `DSParser.parse()` run against the same file is findable in `DSLandmarks.form_bodies`, and that `forms_open`, `forms_close`, `reports_open`, `reports_close` match pinned known-good values computed on first pass of this fixture.

**Acceptance criteria:**
- Spec §7.4 has been updated with the empirical escape-convention finding (commit the spec edit before or alongside the landmarks implementation).
- `pytest tests/test_apply_playbook_landmarks.py -v` passes all test functions including the large-file regression.
- `DSLandmarks` fields `forms_open`, `forms_close`, `reports_open`, `reports_close` are always integers for a valid `.ds`.
- DS062 and DS063 abort with exit 2 in the orchestrator (the landmarks function itself raises or returns them as ERROR-severity diagnostics that the orchestrator treats as fatal).
- The state machine is a single pass — no second scan, no regex over the full file text.

**Out of scope for this task:** IR parsing (playbook markdown). The landmarks module only processes `.ds` text. No `DSParser` import is required here; the large-file regression test imports `DSParser` only in the test file itself.

---

## ### Task T6 — Validator

**Depends on:** T0-prereq, T1, T3, T5

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\validator.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_validator.py`

**Implementation notes:** `validate(ir: PlaybookIR, landmarks: DSLandmarks, ds_parser_result, args) -> tuple[bool, list[Diagnostic]]`. Takes the fixed `DSParser` result (from T0-prereq) for form/field inventory. Runs checks DS021–DS032 in the order listed in spec §9. The `input.<field>` extraction for DS023 uses a regex `input\.(\w+)` applied only to substrings that the string-literal state machine marks as outside string literals — reuse or inline the same escape-convention logic established in T5. For chained accesses like `input.Employee_Name1.first_name`, validate only the first segment. DS030 checks for existing `link_name` values already present in the workflow block of the `.ds` (scan the raw text at `landmarks.workflow_open..workflow_close` range); downgrade to DS031 when `--allow-duplicate-links` is set. DS032 scans for sentinel comments in the workflow block; suppress with `--allow-reapply`. Returns `(True, diags)` if no ERROR-severity diagnostics, `(False, diags)` otherwise.

**Tests to write:** `tests/test_apply_playbook_validator.py` covering all cases from spec §12 validator section: unknown form name → DS021 ERROR; unknown report name → DS022 ERROR; `input.WRONG_CASE_field` → DS023 ERROR; `alert("input.bad_field")` where `bad_field` is not a field → DS023 does NOT fire (false-positive regression); valid IR → no errors; duplicate `link_name` in existing `.ds` without flag → DS030 ERROR; duplicate `link_name` with `--allow-duplicate-links` → DS031 WARNING and `valid=True`; sentinel already present without flag → DS032 WARNING and exit 1; `display_name` with `"` → DS027 ERROR; unbalanced parens in Deluge body → DS028 ERROR; `CustomAction.backing_form` not matching report's underlying form → DS026 ERROR.

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_validator.py -v` passes all test functions.
- DS023 does not fire on `alert("input.bad_field")` — the string-literal exclusion regression test passes.
- `validate()` returns `(False, [...])` for any ERROR-severity diagnostic.
- `validate()` returns `(True, [...])` when only WARNING or INFO diagnostics are present.
- T0-prereq fix is confirmed present (zero-field `FormDef` test in `test_parse_ds_export.py` passes) before this task's test suite is run.

**Out of scope for this task:** Emitter logic, mutator logic, orchestrator wiring. The validator does not write any files or call `to_json_v1`. DS024 is explicitly dropped from v1 per spec §9 and §2.2 — do not implement it.

---

## ### Task T7 — Emitter

**Depends on:** T3

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\emitter.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_emitter.py`

**Implementation notes:** `emit(ir: PlaybookIR, mode: str, playbook_path: str) -> tuple[list[EmitResult], list[Diagnostic]]`. `EmitResult` is a named tuple `(target: str, content: str, insertion_hint: str)` where `target` is a form or report `link_name` and `insertion_hint` indicates whether it goes into `workflow` or a specific `report_bodies` slot. For each `FormAction`, render the workflow-entry block from spec §7.1, prepending the sentinel comment `/* forgeds-apply-playbook: from playbook <basename> sha256=<first-8-of-sha256> */` before the `<link_name> as "<display_name>"` line. For each `CustomAction` in `stub-unwired` mode (default), render the custom-action block from spec §7.2 only — no backing workflow entry, no DS040. For `form` or `standalone` modes, check the spike-gate: if `FORGEDS_APPLY_PLAYBOOK_SPIKE_OVERRIDE_TESTONLY` and `PYTEST_CURRENT_TEST` are both set, emit the backing workflow block per spec §7.3 and append DS040 WARNING; otherwise emit DS043 ERROR and return early. The `record event` line in mode `form` uses `link_name`, not `display_name` (spec §7.3 note). Implementation notes from `DelugeBlock.implementation_notes` are prepended as `/* NOTE from playbook: <text> */` comments inside the `custom deluge script ( ... )` wrapper.

**Tests to write:** `tests/test_apply_playbook_emitter.py` covering: (1) `FormAction` → emitted string matches spec §7.1 shape including sentinel comment; (2) `CustomAction` mode `form` with spike override env vars set → emitted strings include backing workflow with `record event = on custom action <link_name>` (link_name, not display_name) + DS040 WARNING; (3) `CustomAction` mode `stub-unwired` → custom-action block only, no DS040; (4) `CustomAction` mode `form` without spike override → DS043 ERROR, exit 3; (5) `DelugeBlock.implementation_notes` non-empty → `/* NOTE ... */` comment lines appear inside the script wrapper; (6) sentinel comment appears in all emitted workflow entries; (7) `--ir-dump` flag: verify `PlaybookIR` serialises to JSON via `dataclasses.asdict()`.

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_emitter.py -v` passes all 7 test functions.
- Mode `form` without spike override produces DS043 ERROR and does not emit any workflow entry.
- Mode `form` with spike override (`FORGEDS_APPLY_PLAYBOOK_SPIKE_OVERRIDE_TESTONLY=1` + `PYTEST_CURRENT_TEST` set) emits backing workflow block and DS040 WARNING.
- Every emitted workflow entry begins with the sentinel comment line.
- `EmitResult` is importable from `emitter.py` and has `target`, `content`, `insertion_hint` fields.

**Out of scope for this task:** Byte-offset computation (that is the mutator's job). The emitter produces text strings only. No `.ds` file is read here. The emitter does not know about `DSLandmarks`.

---

## ### Task T8 — Mutator

**Depends on:** T5, T7

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\mutator.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_mutator.py`

**Implementation notes:** `mutate(ds_text: str, landmarks: DSLandmarks, emit_results: list[EmitResult]) -> tuple[str, list[Diagnostic]]`. Two strictly-ordered phases per spec §4.2 and §8.3. Phase 1 (Validation): compute all insertion offsets from `DSLandmarks`; run DS050–DS052 guardrails against the full `EmitResult` list; if any ERROR-severity guardrail fires, return the original `ds_text` unchanged and the diagnostic list (atomicity invariant). Phase 2 (Splice): sort `EmitResult` list by insertion offset descending (highest offset first) to avoid index-shift cascading; apply each insertion with `ds_text[:offset] + content + ds_text[offset:]`. If no `workflow { }` block exists (`DSLandmarks.workflow_open is None`), synthesise a `workflow { ... }` block and insert it between `reports_close` and `pages_open` per spec §8.2. Emit DS053 WARNING when `--out` equals `--ds` (in-place mutation); this check is performed in the orchestrator before calling the mutator — the mutator itself does not need file-path awareness. Sentinel detection: scan the raw `ds_text` between `workflow_open` and `workflow_close` for any line matching the sentinel pattern for the current playbook; emit DS032 WARNING if found (unless `--allow-reapply`).

**Tests to write:** `tests/test_apply_playbook_mutator.py` covering: (1) insertion into existing `workflow { }` → content appended immediately before closing `}`; (2) `minimal_no_workflow.ds` → synthesised `workflow { }` block placed between `reports_close` and `pages_open` in output; (3) DS050 guardrail: computed offset falls in the forms-reports gap → DS050 ERROR, output text byte-equals input text; (4) atomicity test: fixture with 5 `EmitResult` objects where the 3rd has an insertion point in the forms-reports gap → assert that the returned text is byte-identical to the original input (no partial splice from items 1–2 applied); (5) reverse-offset ordering: assert that two insertions into the same block produce correct output regardless of the order of `EmitResult` items in the input list.

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_mutator.py -v` passes all 5 test functions.
- Atomicity test (item 4) passes — partial splices must not occur when a guardrail fires on any item in the batch.
- Insertions are applied in descending offset order (confirmed by the reverse-ordering test).
- No disk I/O in the mutator module — it operates on strings only. File writing is the orchestrator's responsibility.
- `workflow { }` synthesis places the new block strictly between `reports_close` and `pages_open`.

**Out of scope for this task:** Orchestrator wiring, file writes, sidecar report generation. The mutator is a pure text-transformation function. DS053 warning for in-place mutation is checked in the orchestrator layer (T9).

---

## ### Task T9 — Orchestrator + E2E

**Depends on:** T0-prereq, T1, T2, T3, T4, T5, T6, T7, T8

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\orchestrator.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\__init__.py` (update stub to real `apply_playbook_main` function)
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_e2e.py`
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\fixtures\apply_playbook\e2e_basic_out.expected.ds` (golden — generated this task)
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\fixtures\apply_playbook\e2e_custom_action_out.expected.ds` (golden — generated this task)

**Implementation notes:** `orchestrator.py` exports `run_apply_playbook(args) -> tuple[str, list[Diagnostic]]`. The pipeline sequence is: (1) read `--ds` and `--playbook` files; (2) run `DSParser.parse()` from `parse_ds_export` for form/field inventory (using the T0-prereq fixed version); (3) run `DSLandmarks` from `landmarks.py`; (4) run `PlaybookParser` from `parser.py`; (5) run `validate()` from `validator.py`; (6) if `valid` and not `--dry-run`, run `emit()` from `emitter.py`; (7) if not `--dry-run`, run `mutate()` from `mutator.py`; (8) if not `--dry-run` and not any ERROR diagnostic, write `--out` file and sidecar report. The sidecar report is a markdown file at `<stem>-apply-playbook-report.md` listing every action taken, every diagnostic (code, severity, message, line), and exact byte offsets of each insertion. Use `to_json_v1` from `forgeds._shared.envelope` with `tool="forgeds-apply-playbook"` for `--json` output. `--json` output goes to stdout; human-readable output goes to stderr (matches convention in other ForgeDS linters). DS053 WARNING is emitted here if `--out` equals `--ds`. After running E2E-1 successfully, hand-audit the `e2e_basic_out.ds` diff against `e2e_basic.ds`, confirm correctness, and commit it as `e2e_basic_out.expected.ds`. Repeat for `e2e_custom_action_out.expected.ds`. See spec §12 E2E cases for full command + assert specification.

**Tests to write:** `tests/test_apply_playbook_e2e.py` covering the four E2E cases from spec §12: E2E-1 happy path (exit 0, output contains expected workflow entry, sidecar exists); E2E-2 field case mismatch (exit 2, DS023 in diagnostics, output file not created); E2E-3 custom-action UNVERIFIED mode with spike override (exit 1, DS040 in diagnostics, emitted `.ds` contains `on custom action <link_name>`); E2E-4 spike gate without override (exit 3, DS043 in diagnostics, no `.ds` written). E2E-1 must also assert golden-file equality against `e2e_basic_out.expected.ds` once that file is committed.

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_e2e.py -v` passes all four E2E tests.
- E2E-1 produces exit 0 and the output `.ds` is byte-identical to `e2e_basic_out.expected.ds`.
- E2E-2 produces exit 2, DS023 is present in diagnostic output, and no output `.ds` file is created.
- E2E-3 produces exit 1 and DS040 is present; the emitted workflow entry's `record event` line uses `link_name` not `display_name`.
- E2E-4 produces exit 3 and DS043 is present; no output file is created.
- `--json` output goes to stdout; all human-readable output goes to stderr.
- `--dry-run` flag on `apply` skips all file writes and produces exit 0 when the pipeline is clean.

**Out of scope for this task:** CLI flag wiring (that is T10). The orchestrator is called from test code directly, not via the CLI entry point. The ERM canonical E2E (mentioned in the brief's task description) is deferred — the `erm_snapshot_sha256.ds` fixture is used for landmarks regression only, not for a full pipeline E2E run, as the golden output would be too large to hand-audit in this task.

---

## ### Task T10 — CLI + docs + CLAUDE.md

**Depends on:** T9

**Files to create / modify:**
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\src\forgeds\core\apply_playbook\cli.py` (replace stubs with real orchestrator calls)
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\CLAUDE.md` (add DS### rule-code table; add one-line toolchain note)
- `C:\Users\User\OneDrive\Documents\Claude\Projects\VS_Clones\ForgeDS\tests\test_apply_playbook_cli_stub.py` (rename to `test_apply_playbook_cli.py`; expand tests)

**Implementation notes:** Wire `cli.py` subcommands to `run_apply_playbook()` from `orchestrator.py`. Exit-code discipline per spec §11: exit 0 = clean, 1 = warnings, 2 = errors (no `.ds` written), 3 = spike-gate exit. `--json` flag passes through to orchestrator which controls stdout/stderr routing. Update `CLAUDE.md` Rule code registry by adding a new `DS###` row under the existing table with prefix `DS###`, owner `forgeds.core.apply_playbook`, and the range/meaning column copied from spec §10. Add one sentence to the "Development workflow" section listing `forgeds-apply-playbook` and its three subcommands. `--help` text on each subcommand must include the re-import warning: "Intended workflow: apply → import to Creator → re-export from Creator → apply again on fresh export." `--ir-dump` flag writes `PlaybookIR` as JSON via `dataclasses.asdict()` to the specified path immediately after parsing (before validation), even on validation failure.

**Tests to write:** Extend (and rename) `tests/test_apply_playbook_cli_stub.py` to `tests/test_apply_playbook_cli.py`: (1) `test_help_exits_zero` (from stub, unchanged); (2) `test_validate_subcommand_exit_zero` — invoke `forgeds-apply-playbook validate --ds <e2e_basic.ds> --playbook <e2e_basic_playbook.md>` via subprocess, assert exit 0; (3) `test_preview_subcommand_prints_to_stdout` — invoke `forgeds-apply-playbook preview --ds <e2e_basic.ds> --playbook <e2e_basic_playbook.md>`, assert exit 0 and non-empty output; (4) `test_apply_error_exits_2` — invoke apply with `e2e_case_mismatch.ds` and `e2e_case_mismatch_playbook.md`, assert exit 2; (5) `test_json_flag_stdout_is_valid_json` — invoke validate with `--json`, parse stdout as JSON, assert `tool == "forgeds-apply-playbook"`.

**Acceptance criteria:**
- `pytest tests/test_apply_playbook_cli.py -v` passes all 5 test functions.
- `forgeds-apply-playbook --help` prints the re-import workflow note.
- `CLAUDE.md` rule-code registry table contains a `DS###` row.
- `forgeds-apply-playbook validate --ds e2e_basic.ds --playbook e2e_basic_playbook.md` exits 0 from the command line (not just via Python).
- `--json` stdout is parseable JSON with `"tool": "forgeds-apply-playbook"` and `"version": "1"`.
- `--ir-dump` writes a valid JSON file even when validation subsequently fails.

**Out of scope for this task:** Any new diagnostic codes or new pipeline logic. T10 is wiring and documentation only. No changes to orchestrator, validator, emitter, or mutator.

---

## Simulation-ready summary

This section lists all 11 tasks with single-line descriptions for Phase 6 role-play stub workers.

| ID | Short name | One-line description |
|---|---|---|
| T0-prereq | DSParser zero-field fix | 3-line fix in `parse_ds_export.py` so forms with no fields produce a `FormDef` with `fields=[]` instead of being silently dropped. |
| T1 | Scaffold package + CLI stub | Create the `apply_playbook` package directory, empty stubs in `__init__.py`, argparse skeleton in `cli.py` with all three subcommands, and register the `forgeds-apply-playbook` entry point in `pyproject.toml`. |
| T2 | Fixture generation | Author all 15 fixture files under `tests/fixtures/apply_playbook/` — valid `.ds` files, playbook markdown files, intentionally-malformed files, and the large ERM snapshot fixture. |
| T3 | IR + diagnostics codes | Define the five IR dataclasses (`PlaybookIR`, `FormAction`, `CustomAction`, `DelugeBlock`, `DeferredSection`) in `ir.py` and all allocated DS### string constants in `diagnostics_codes.py`. |
| T4 | Parser | Implement `PlaybookParser` in `parser.py` that walks playbook markdown and produces a `PlaybookIR` with DS001–DS015 diagnostics. |
| T5 | Landmarks | Implement the `DSLandmarks` brace-depth state machine in `landmarks.py`, including the §7.4 empirical escape-convention study against the real ERM `.ds` file, and the large-file regression test. |
| T6 | Validator | Implement `validate()` in `validator.py` that cross-checks IR against `DSLandmarks` + fixed `DSParser` inventory, emitting DS021–DS032 diagnostics. |
| T7 | Emitter | Implement `emit()` in `emitter.py` that renders IR objects to `.ds` text strings, including the spike-gate for `form`/`standalone` modes and the sentinel comment on every workflow entry. |
| T8 | Mutator | Implement `mutate()` in `mutator.py` with two-phase validation-then-splice discipline, reverse-offset ordering, placement guardrails DS050–DS052, and `workflow { }` synthesis when the block is absent. |
| T9 | Orchestrator + E2E | Wire the full pipeline in `orchestrator.py`, generate and hand-audit the two golden output fixtures, and implement the four E2E test cases from spec §12. |
| T10 | CLI + docs + CLAUDE.md | Replace CLI stubs with real orchestrator calls, enforce exit-code discipline, update `CLAUDE.md` with DS### rule-code table, and add `--help` re-import workflow note. |
